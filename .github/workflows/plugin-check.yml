name: Plugin Check

on:
    push:
        branches:
            - main
    pull_request:

# Cancels all previous workflow runs for the same branch that have not yet completed.
concurrency:
    # The concurrency group contains the workflow name and the branch name.
    group: ${{ github.workflow }}-${{ github.ref }}
    cancel-in-progress: true

permissions: {}

jobs:
    plugin-check:
        name: Plugin Check
        runs-on: ubuntu-latest

        steps:
            - name: Checkout
              uses: actions/checkout@v7

            - name: Setup Node
              uses: actions/setup-node@v7
              with:
                  node-version-file: '.nvmrc'
                  cache: npm

            - name: Install dependencies
              run: npm ci

            - name: Build plugin
              run: npm run build

            # Not wp-cli:nightly: see the comment on the same step in
            # nightly-build.yml — its nightly phar isn't hosted as a GitHub
            # Releases asset, so setup-php can't fetch it.
            #
            # github-token: '' — see the comment on the same step in
            # nightly-build.yml: setup-php writes this into Composer's
            # global, job-wide auth.json while installing wp-cli, and a
            # later step needs that file to *not* contain a token.
            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: latest
                  coverage: none
                  tools: wp-cli
                  github-token: ''

            # Pinned to 3.1.0, not :@stable: see the comment on the same
            # step in nightly-build.yml — 3.2.0+ requires a wp-cli/wp-cli
            # version that has never actually been released.
            - name: Install compatible version of dist-archive-command
              run: wp package install wp-cli/dist-archive-command:3.1.0

            # Plugin Check should see exactly what a directory submission
            # would ship — not the dev sources (src, tests, vendor's dev-only
            # tooling) that only exist in this repo. `wp dist-archive` builds
            # that from .distignore, same as the nightly build does.
            - name: Build distributable copy
              run: |
                  wp dist-archive . ./upload-from-phone.zip
                  mkdir dist
                  unzip upload-from-phone.zip -d dist

            # Defaults to the current stable release, which is older than the
            # plugin's minimum supported WordPress version while that version
            # is still an RC — WordPress core itself then refuses to activate
            # the plugin at all. trunk tracks core development and stays
            # ahead of any unreleased "Requires at least".
            - name: Run Plugin Check
              uses: wordpress/plugin-check-action@v1
              with:
                  build-dir: './dist/upload-from-phone'
                  wp-version: 'trunk'
